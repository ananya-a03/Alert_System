<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <meta name="viewport" 
          content="width=device-width, 
                   initial-scale=1.0">
<link rel="stylesheet" href="index.css">
    <script>
        function fun(){
            document.getElementById('fname').value='';
            document.getElementById('phone').value='';
            }
            const inputField = document.getElementById('phone');
           
  // Add an event listener to the input field to check its length
            inputField.addEventListener('input', function() {
            const inputValue = inputField.value;
            const inputLength = inputValue.length;

            if (inputLength != 12) {
            lengthMessage.textContent = 'Please enter a value.';
            } 
        });
    </script>

   
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            background:#e7c0c0;
            background: linear-gradient(120deg, rgb(189, 56, 56)  50%, #111110 50%);
        }

        .container {
            background-image: url("CM3.jpg");
            background-size: cover;
            background-position: center;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .form {
            background-color: rgba(1, 0, 0, 0.8);
            width: 400px;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
        }

        .form h2 {
            color: white;
            text-align: center;
        }

        .form label {
            color: white;
            display: block;
            margin-bottom: 5px;
        }

        .form input[type="text"] {
            width: 95%;
            padding: 8px;
            margin-bottom: 15px;
            border: none;
            border-radius: 5px;
        }

        .form input[type="submit"] {
            background-color: rgba(255, 85, 85, 0.7);
            color: white;
            border: none;
            padding: 10px 15px;
            border-radius: 5px;
            cursor: pointer;
            width: 100%;
        }
        .link {
            position : absolute;
            top : 20px;
            right : 20px;
            /*color: rgb(250, 253, 250);
            background-color: transparent;*/
        }
        p{
            color : white;
            font-style: bold;
            size:20px;

        }
       

    </style>
    
</head>

<body>
    <div class="container">
        <div class="form">
            <h2>Contact Details</h2>
            <form id = form method="POST" action="/data_insert">
                <label for="fname">Full Name:</label>
                <input type="text" id="fname" name="fname" value=" ">

                <label for="phone">Contact Number:</label>
                <input type="text" id="phone" name="phone" value=" " >

                <input  type="submit"value="Insert">
                
                
                <p> </p>
                <p id="demo" onclick="fun()">Clear</p>
                
            </form>
        </div>
    </div>
    <div class = link>
    <a href="/message_box" style = "color :rgb(250, 250, 250)">Send Message</a>
   </div>


</body>

</html>

